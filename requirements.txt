# Basketball CV - Real-time stat tracking system
# Python 3.11+ required

# Core computer vision and ML
ultralytics>=8.0.0        # YOLOv11, ByteTrack, pose estimation
opencv-python>=4.8.0      # Camera capture, preprocessing, homography
numpy>=1.24.0             # Array operations

# Note: PyTorch + CUDA installed separately via:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# (cu121 does not have Python 3.13 wheels — use cu124)

# Configuration and logging
pyyaml>=6.0              # config.yaml parsing

# Data handling and export (Phase 2)
pandas>=2.0.0            # Stat aggregation and export
openpyxl>=3.1.0          # Excel export

# Scoreboard UI (Phase 1.5)
pygame>=2.5.0            # Real-time overlay rendering
PyQt6>=6.5.0             # Desktop app UI (app.py)

# Privacy / face blur (post-processing via blur_footage.py)
sam2>=1.1.0              # SAM2 video predictor — pixel-precise face mask propagation
transformers>=4.52.0     # SAM3 support (future upgrade path once Meta grants access)
# insightface             # SCRFD detector (better than YOLOv8-face but requires MSVC on Windows)
# Note: YOLOv8-face weights auto-downloaded from HuggingFace (arnabdhar/YOLOv8-Face-Detection)
# Note: SAM2 weights auto-downloaded from HuggingFace (facebook/sam2.1-hiera-large) — not gated

# Alternative detection backend (uncomment to use RF-DETR)
# rfdetr                  # RF-DETR-L — transformer detection backbone, better occlusion handling
#                         # Set config.yaml model.backend: rfdetr to activate
#                         # Note: XL/2XL sizes require a Roboflow subscription (PML license)

# Optional dependencies (uncomment if needed)
# roboflow                # Access to basketball datasets for fine-tuning (not needed at runtime)
# matplotlib>=3.7.0       # Charts in Excel export
# pillow>=10.0.0          # Image manipulation (included with ultralytics)
