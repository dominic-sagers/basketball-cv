# Development / test dependencies (not needed at runtime).
#   pip install -r requirements-dev.txt
#
# Runtime deps live in requirements.txt. These are kept separate so the
# gym/inference install stays lean and internet-free.

pytest>=8.0           # test runner
httpx>=0.27           # required by Starlette's TestClient (endpoint tests)
